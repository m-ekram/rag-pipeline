# --- core RAG stack ---
langchain>=1.0.0
langchain-core>=1.0.0
langchain-classic>=1.0.0          # EnsembleRetriever, ContextualCompressionRetriever, CrossEncoderReranker
langchain-community>=0.4.0        # FAISS wrapper, BM25Retriever
langchain-text-splitters>=1.0.0
langchain-openai>=1.0.0           # default provider: gpt-4o-mini + text-embedding-3-small
faiss-cpu>=1.8.0
numpy>=1.26.0

# --- local embeddings + cross-encoder re-ranking ---
# ONNX Runtime, not PyTorch: ~10x smaller install, which is what keeps the
# Docker image small enough for a modest EC2 instance.
fastembed>=0.5.0

# --- optional hosted alternative ---
langchain-google-genai>=2.0.0

# --- document loading ---
pypdf>=4.2.0
beautifulsoup4>=4.12.0
python-docx>=1.1.0

# --- serving ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
sse-starlette>=2.1.0

# --- misc ---
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Offline chat on llama.cpp lives in requirements-local-llm.txt;
# benchmark tooling in requirements-dev.txt.
